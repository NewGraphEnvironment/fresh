% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_feature_index.R
\name{frs_feature_index}
\alias{frs_feature_index}
\title{Index Upstream/Downstream Features for Stream Segments}
\usage{
frs_feature_index(
  conn,
  segments,
  features,
  direction = "downstream",
  col_segment_id = "id_segment",
  col_feature_id = "feature_id",
  to = "working.feature_index",
  label_filter = NULL,
  overwrite = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{segments}{Character. Schema-qualified segmented streams table
(from \code{\link[=frs_network_segment]{frs_network_segment()}}).}

\item{features}{Character. Schema-qualified feature table
(from \code{\link[=frs_feature_find]{frs_feature_find()}}).}

\item{direction}{Character. \code{"downstream"} (features between segment
and ocean) or \code{"upstream"} (features above segment). Default
\code{"downstream"}.}

\item{col_segment_id}{Character. Segment ID column. Default
\code{"id_segment"}.}

\item{col_feature_id}{Character. Feature ID column. Default
\code{"feature_id"}. If the feature table has no ID column, uses
row position.}

\item{to}{Character. Output table name. Default
\code{"working.feature_index"}.}

\item{label_filter}{Character or \code{NULL}. SQL predicate to filter
features by label before indexing (e.g. \code{"label = 'blocked'"}).}

\item{overwrite}{Logical. Drop \code{to} before creating. Default \code{TRUE}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
For each segment in a stream table, find which features from a
feature table are upstream or downstream on the network. Stores
results as feature ID arrays — enabling queries like "which
crossings are between this segment and the ocean?"
}
\details{
Uses \code{fwa_upstream()} or \code{fwa_downstream()} for network-aware
traversal via ltree codes.
}
\examples{
\dontrun{
conn <- frs_db_conn()

# Which crossings are downstream of each segment?
frs_feature_index(conn,
  segments = "fresh.streams",
  features = "working.features_crossings",
  direction = "downstream",
  to = "working.crossings_dnstr")

# Query: segments with confirmed barriers downstream
DBI::dbGetQuery(conn, "
  SELECT s.id_segment, i.features_dnstr
  FROM fresh.streams s
  JOIN working.crossings_dnstr i ON s.id_segment = i.id_segment
  WHERE array_length(i.features_dnstr, 1) > 0
  LIMIT 10")

# Fish observations upstream of each segment
frs_feature_index(conn,
  segments = "fresh.streams",
  features = "working.features_fish_obs",
  direction = "upstream",
  col_feature_id = "fish_observation_point_id",
  to = "working.fish_obs_upstr")

DBI::dbDisconnect(conn)
}
}
\seealso{
Other habitat: 
\code{\link{frs_aggregate}()},
\code{\link{frs_break}()},
\code{\link{frs_break_apply}()},
\code{\link{frs_break_find}()},
\code{\link{frs_break_validate}()},
\code{\link{frs_categorize}()},
\code{\link{frs_classify}()},
\code{\link{frs_col_generate}()},
\code{\link{frs_col_join}()},
\code{\link{frs_extract}()},
\code{\link{frs_feature_find}()},
\code{\link{frs_habitat}()},
\code{\link{frs_habitat_access}()},
\code{\link{frs_habitat_classify}()},
\code{\link{frs_habitat_partition}()},
\code{\link{frs_habitat_species}()},
\code{\link{frs_network_segment}()}
}
\concept{habitat}
