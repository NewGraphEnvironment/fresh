% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_wetland_fetch.R
\name{frs_wetland_fetch}
\alias{frs_wetland_fetch}
\title{Fetch FWA Wetlands}
\usage{
frs_wetland_fetch(
  watershed_group_code = NULL,
  blue_line_key = NULL,
  bbox = NULL,
  area_ha_min = NULL,
  limit = NULL,
  ...
)
}
\arguments{
\item{watershed_group_code}{Character. Watershed group code (e.g. \code{"BULK"}).
Default \code{NULL}.}

\item{blue_line_key}{Integer. Blue line key for wetlands on a specific stream.
Default \code{NULL}.}

\item{bbox}{Numeric vector of length 4 (\code{xmin}, \code{ymin}, \code{xmax}, \code{ymax}) in
BC Albers (EPSG:3005). Default \code{NULL}.}

\item{area_ha_min}{Numeric. Minimum wetland area in hectares. Default \code{NULL}.}

\item{limit}{Integer. Maximum rows to return. Default \code{NULL} (no limit).}

\item{...}{Additional arguments passed to \code{\link[=frs_db_conn]{frs_db_conn()}}.}
}
\value{
An \code{sf} data frame of wetland polygons.
}
\description{
Retrieve wetland polygons from \code{whse_basemapping.fwa_wetlands_poly}.
Filter by watershed group code, blue line key, and/or bounding box.
}
\examples{
\dontrun{
# All wetlands in the Bulkley watershed group
wetlands <- frs_wetland_fetch(watershed_group_code = "BULK")

# Wetlands larger than 5 ha
wetlands <- frs_wetland_fetch(watershed_group_code = "BULK", area_ha_min = 5)
}
}
\seealso{
Other fetch: 
\code{\link{frs_lake_fetch}()},
\code{\link{frs_stream_fetch}()}
}
\concept{fetch}
