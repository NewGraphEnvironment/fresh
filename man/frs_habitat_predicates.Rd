% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_habitat_predicates.R
\name{frs_habitat_predicates}
\alias{frs_habitat_predicates}
\title{Build SQL predicates for one species' habitat classification}
\usage{
frs_habitat_predicates(sp_params)
}
\arguments{
\item{sp_params}{A single-species params list as produced by one
element of \code{\link[=frs_habitat_species]{frs_habitat_species()}}. Must contain \code{species_code},
\code{spawn_gradient_min}, \code{spawn_gradient_max}, \code{ranges}, optionally
\code{rules}, optionally \code{spawn_edge_types} / \code{rear_edge_types}.}
}
\value{
A named list with four character scalars: \code{spawn}, \code{rear},
\code{lake_rear}, \code{wetland_rear}. Each is an SQL boolean expression
suitable for embedding in \verb{CASE WHEN ... THEN TRUE ELSE FALSE END}. Predicates reference the segmented streams alias \code{s.}.
}
\description{
Pure-R helper: takes one species' rules + ranges from
\code{\link[=frs_habitat_species]{frs_habitat_species()}} and returns a named list of SQL boolean
expressions ("predicates") — the raw yes/no questions that
\code{\link[=frs_habitat_classify]{frs_habitat_classify()}} embeds in \verb{CASE WHEN <pred> THEN TRUE ...}
to produce the per-species habitat columns.
}
\details{
Returns four predicates per call: \code{spawn}, \code{rear}, \code{lake_rear},
\code{wetland_rear}. Each is a fragment that references columns aliased
as \code{s.} (the segmented streams table). Caller is responsible for
embedding them in a complete query.

Two paths are supported, selected per habitat type by what's
present in \code{sp_params}:
\enumerate{
\item \strong{Rules path} — when \verb{sp_params$rules$<spawn|rear>} is non-NULL,
the rules YAML is compiled to SQL via \code{\link[=.frs_rules_to_sql]{.frs_rules_to_sql()}}.
CSV thresholds (gradient + channel_width) are passed as the
inheritance fallback for rules that omit explicit thresholds.
\item \strong{CSV-ranges path} — pre-rules behaviour. Builds the SQL
directly from \code{sp_params$ranges} + \verb{sp_params$<spawn|rear>_edge_types}.
}

Lake / wetland rearing predicates are gated on the presence of a
\code{waterbody_type: L} / \code{waterbody_type: W} rule in \verb{rear:}. Without
the rule, the predicate is \code{"FALSE"} — the species is not lake or
wetland-rearing. With the rule, an optional \code{lake_ha_min} /
\code{wetland_ha_min} filters the polygon join.

Segments must still fall within the species' rear channel-width
window for lake / wetland rearing.
}
\examples{
\dontrun{
params <- frs_params()
params_fresh <- read.csv(system.file("extdata",
  "parameters_fresh.csv", package = "fresh"))
species_params <- frs_habitat_species("CO", params, params_fresh)

preds <- frs_habitat_predicates(species_params[[1]])
preds$spawn
#> "s.gradient >= 0 AND s.gradient <= 0.0549 AND ..."
preds$lake_rear
#> "FALSE"  (CO has no waterbody_type: L rule under bcfishpass)
}
}
\seealso{
Other habitat: 
\code{\link{frs_aggregate}()},
\code{\link{frs_break}()},
\code{\link{frs_break_apply}()},
\code{\link{frs_break_find}()},
\code{\link{frs_break_validate}()},
\code{\link{frs_categorize}()},
\code{\link{frs_classify}()},
\code{\link{frs_cluster}()},
\code{\link{frs_col_generate}()},
\code{\link{frs_col_join}()},
\code{\link{frs_extract}()},
\code{\link{frs_feature_find}()},
\code{\link{frs_feature_index}()},
\code{\link{frs_habitat}()},
\code{\link{frs_habitat_access}()},
\code{\link{frs_habitat_classify}()},
\code{\link{frs_habitat_overlay}()},
\code{\link{frs_habitat_partition}()},
\code{\link{frs_habitat_species}()},
\code{\link{frs_network_segment}()}
}
\concept{habitat}
