% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_habitat.R
\name{frs_habitat_species}
\alias{frs_habitat_species}
\title{Classify Habitat for One Species}
\usage{
frs_habitat_species(
  conn,
  species_code,
  base_tbl,
  breaks,
  breaks_habitat = NULL,
  params_sp,
  fresh_sp,
  to = NULL
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object (from \code{\link[=frs_db_conn]{frs_db_conn()}}).}

\item{species_code}{Character. Uppercase species code (e.g. \code{"CO"}, \code{"BT"}).}

\item{base_tbl}{Character. Schema-qualified base table with the enriched
stream network (from \code{\link[=frs_extract]{frs_extract()}} + \code{\link[=frs_col_join]{frs_col_join()}}).}

\item{breaks}{Character. Schema-qualified access breaks table from
\code{\link[=frs_habitat_access]{frs_habitat_access()}}.}

\item{breaks_habitat}{Character or \code{NULL}. Schema-qualified habitat gradient
breaks table. When provided, skips the per-species gradient scan and
applies this pre-computed table instead. Default \code{NULL} computes on the
fly.}

\item{params_sp}{Named list. Species parameters from \code{\link[=frs_params]{frs_params()}}
(e.g. \code{frs_params()$CO}).}

\item{fresh_sp}{Data frame row. Species row from \code{parameters_fresh.csv}
with \code{access_gradient_max}, \code{spawn_gradient_min}.}

\item{to}{Character or \code{NULL}. Output table name. Default \code{NULL} uses
\verb{working.streams_\{sp\}}.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Copy a base stream network, apply pre-computed access barriers, then
classify spawning, rearing, and lake rearing habitat for a single species.
Each species gets its own output table because break points modify segment
geometry.
}
\examples{
\dontrun{
conn <- frs_db_conn()
params <- frs_params(csv = system.file("extdata",
  "parameters_habitat_thresholds.csv", package = "fresh"))
fresh <- read.csv(system.file("extdata",
  "parameters_fresh.csv", package = "fresh"))

frs_habitat_species(conn, "CO", "working.streams_bulk",
  breaks = "working.breaks_access_bulk_015",
  breaks_habitat = "working.breaks_habitat_bulk_00549",
  params_sp = params$CO,
  fresh_sp = fresh[fresh$species_code == "CO", ],
  to = "working.streams_bulk_co")

DBI::dbDisconnect(conn)
}
}
\seealso{
Other habitat: 
\code{\link{frs_aggregate}()},
\code{\link{frs_break}()},
\code{\link{frs_break_apply}()},
\code{\link{frs_break_find}()},
\code{\link{frs_break_validate}()},
\code{\link{frs_categorize}()},
\code{\link{frs_classify}()},
\code{\link{frs_col_generate}()},
\code{\link{frs_col_join}()},
\code{\link{frs_extract}()},
\code{\link{frs_habitat}()},
\code{\link{frs_habitat_access}()},
\code{\link{frs_habitat_partition}()}
}
\concept{habitat}
