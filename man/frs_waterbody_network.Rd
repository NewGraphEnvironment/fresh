% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_waterbody_network.R
\name{frs_waterbody_network}
\alias{frs_waterbody_network}
\title{Get Waterbody Polygons Upstream or Downstream of a Network Position}
\usage{
frs_waterbody_network(
  blue_line_key,
  downstream_route_measure,
  table = "whse_basemapping.fwa_lakes_poly",
  cols = c("waterbody_key", "waterbody_type", "gnis_name_1", "area_ha", "blue_line_key",
    "watershed_group_code", "geom"),
  direction = "upstream",
  ...
)
}
\arguments{
\item{blue_line_key}{Integer. Blue line key of the reference point.}

\item{downstream_route_measure}{Numeric. Downstream route measure of the
reference point.}

\item{table}{Character. Fully qualified polygon table name. Default
\code{"whse_basemapping.fwa_lakes_poly"}.}

\item{cols}{Character vector of column names to select from the polygon
table. Default includes the most commonly used attributes.}

\item{direction}{Character. \code{"upstream"} (default) or \code{"downstream"}.}

\item{...}{Additional arguments passed to \code{\link[=frs_db_conn]{frs_db_conn()}}.}
}
\value{
An \code{sf} data frame of waterbody polygons.
}
\description{
Returns lake or wetland polygons upstream or downstream of a given blue line
key and downstream route measure. Polygon tables (\code{fwa_lakes_poly},
\code{fwa_wetlands_poly}) have NULL \code{localcode_ltree}, so \code{fwa_upstream()} /
\code{fwa_downstream()} can't query them directly. This function bridges through
the stream network: it runs the traversal on stream segments (which have
\code{localcode_ltree}), extracts distinct \code{waterbody_key} values, then joins to
the polygon table.
}
\details{
See \href{https://github.com/NewGraphEnvironment/fresh/issues/8}{fresh#8} for
background.
}
\examples{
\dontrun{
# Upstream lakes from the Neexdzii Kwa / Wedzin Kwa confluence
lakes <- frs_waterbody_network(
  blue_line_key = 360873822,
  downstream_route_measure = 166030
)

# Upstream wetlands
wetlands <- frs_waterbody_network(
  blue_line_key = 360873822,
  downstream_route_measure = 166030,
  table = "whse_basemapping.fwa_wetlands_poly"
)

# Downstream lakes
lakes_ds <- frs_waterbody_network(
  blue_line_key = 360873822,
  downstream_route_measure = 166030,
  direction = "downstream"
)
}
}
\seealso{
Other traverse: 
\code{\link{frs_network}()},
\code{\link{frs_network_downstream}()},
\code{\link{frs_network_upstream}()}
}
\concept{traverse}
