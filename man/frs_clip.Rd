% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_clip.R
\name{frs_clip}
\alias{frs_clip}
\title{Clip Spatial Features to an AOI Polygon}
\usage{
frs_clip(x, aoi)
}
\arguments{
\item{x}{An \code{sf} data frame to clip.}

\item{aoi}{An \code{sf} or \code{sfc} polygon to clip to.}
}
\value{
An \code{sf} data frame clipped to \code{aoi}, with geometry type matching
the input. Returns an empty \code{sf} with the same columns if no features
intersect.
}
\description{
Clips an \code{sf} data frame to an area of interest polygon using
\code{sf::st_intersection()}. Handles geometry type cleanup automatically —
mixed geometry collections from intersection are filtered to the original
geometry type (e.g. polygon, linestring).
}
\details{
Typical use: clip network query results (lakes, wetlands, streams) to a
watershed polygon from \code{\link[=frs_watershed_at_measure]{frs_watershed_at_measure()}}.
}
\examples{
\dontrun{
# Clip wetlands to a subbasin watershed
aoi <- frs_watershed_at_measure(blk, drm, upstream_measure = urm)
wetlands <- frs_network(blk, drm, tables = list(
  wetlands = "whse_basemapping.fwa_wetlands_poly"
))
wetlands_clipped <- frs_clip(wetlands, aoi)
}
}
\concept{spatial}
