% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_lake_fetch.R
\name{frs_lake_fetch}
\alias{frs_lake_fetch}
\title{Fetch FWA Lakes}
\usage{
frs_lake_fetch(
  watershed_group_code = NULL,
  blue_line_key = NULL,
  bbox = NULL,
  area_ha_min = NULL,
  table = "whse_basemapping.fwa_lakes_poly",
  cols = c("waterbody_poly_id", "waterbody_key", "waterbody_type", "area_ha",
    "gnis_name_1", "blue_line_key", "watershed_group_code", "geom"),
  limit = NULL,
  ...
)
}
\arguments{
\item{watershed_group_code}{Character. Watershed group code (e.g. \code{"BULK"}).
Default \code{NULL}.}

\item{blue_line_key}{Integer. Blue line key for lakes on a specific stream.
Default \code{NULL}.}

\item{bbox}{Numeric vector of length 4 (\code{xmin}, \code{ymin}, \code{xmax}, \code{ymax}) in
BC Albers (EPSG:3005). Default \code{NULL}.}

\item{area_ha_min}{Numeric. Minimum lake area in hectares. Default \code{NULL}.}

\item{table}{Character. Fully qualified table name. Default
\code{"whse_basemapping.fwa_lakes_poly"}.}

\item{cols}{Character vector of column names to select. Default includes
the most commonly used FWA lake attributes.}

\item{limit}{Integer. Maximum rows to return. Default \code{NULL} (no limit).}

\item{...}{Additional arguments passed to \code{\link[=frs_db_conn]{frs_db_conn()}}.}
}
\value{
An \code{sf} data frame of lake polygons.
}
\description{
Retrieve lake polygons from an FWA lakes table.
Filter by watershed group code, blue line key, and/or bounding box.
}
\examples{
\dontrun{
# All lakes in the Bulkley watershed group
lakes <- frs_lake_fetch(watershed_group_code = "BULK")

# Lakes larger than 10 ha
lakes <- frs_lake_fetch(watershed_group_code = "BULK", area_ha_min = 10)
}
}
\seealso{
Other fetch: 
\code{\link{frs_stream_fetch}()},
\code{\link{frs_wetland_fetch}()}
}
\concept{fetch}
