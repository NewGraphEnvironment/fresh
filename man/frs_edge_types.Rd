% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_edge_types.R
\name{frs_edge_types}
\alias{frs_edge_types}
\title{FWA Edge Type Lookup Table}
\usage{
frs_edge_types(category = NULL)
}
\arguments{
\item{category}{Character or \code{NULL}. If provided, filter to rows matching
this category. One of \code{"lake"}, \code{"wetland"}, \code{"river"}, \code{"stream"},
\code{"subsurface"}, \code{"connector"}, \code{"construction"}, \code{"boundary"},
\code{"unknown"}. Default \code{NULL} returns all rows.}
}
\value{
A data.frame with columns \code{edge_type} (integer), \code{description}
(character), and \code{category} (character).
}
\description{
Return the FWA edge type codes bundled with this package, optionally
filtered by category. Source: Table 2 "Edge Type Code Table" from the
GeoBC Freshwater Atlas User Guide (GeoBC 2009, p. 11).
}
\examples{
# All edge types
frs_edge_types()

# Just lake codes
frs_edge_types(category = "lake")

# Stream-type codes (definite, probable, intermittent, inferred)
frs_edge_types(category = "stream")

# Use with frs_classify() to scope classification by waterbody type
lake_codes <- frs_edge_types(category = "lake")$edge_type
paste("edge_type IN (", paste(lake_codes, collapse = ", "), ")")
}
\concept{reference}
