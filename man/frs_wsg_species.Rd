% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_wsg_species.R
\name{frs_wsg_species}
\alias{frs_wsg_species}
\title{Species Present in a Watershed Group}
\usage{
frs_wsg_species(watershed_group_code)
}
\arguments{
\item{watershed_group_code}{Character. One or more watershed group codes
(e.g. \code{"BULK"}, \code{c("BULK", "MORR")}).}
}
\value{
A data frame with columns:
\describe{
\item{watershed_group_code}{Watershed group code}
\item{species_code}{Uppercase species code (e.g. \code{"CO"}, \code{"BT"})}
\item{view}{bcfishpass view name (e.g.
\code{"bcfishpass.streams_co_vw"}), or \code{NA} for species without a view}
}
}
\description{
Look up which species have bcfishpass habitat models for a given watershed
group code. Returns species codes and their corresponding bcfishpass view
names. Based on the
\href{https://github.com/smnorris/bcfishpass/blob/main/data/wsg_species_presence.csv}{wsg_species_presence.csv}
bundled in the package.
}
\details{
Some species share a combined view in bcfishpass: cutthroat trout (\code{ct}),
Dolly Varden (\code{dv}), and rainbow trout (\code{rb}) all use
\code{streams_ct_dv_rb_vw}. Arctic grayling (\code{gr}) has no bcfishpass view and
is excluded from view mapping.
}
\examples{
# Which species are modelled in the Bulkley watershed group?
frs_wsg_species("BULK")

# Multiple watershed groups
frs_wsg_species(c("BULK", "MORR"))

# Just the unique views needed for BULK
sp <- frs_wsg_species("BULK")
unique(sp$view[!is.na(sp$view)])
}
\seealso{
Other parameters: 
\code{\link{frs_params}()}
}
\concept{parameters}
