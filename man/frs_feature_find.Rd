% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_feature_find.R
\name{frs_feature_find}
\alias{frs_feature_find}
\title{Find Features on a Stream Network}
\usage{
frs_feature_find(
  conn,
  table,
  to = "working.features",
  points_table = NULL,
  points = NULL,
  where = NULL,
  col_blk = "blue_line_key",
  col_measure = "downstream_route_measure",
  col_id = NULL,
  label = NULL,
  label_col = NULL,
  label_map = NULL,
  overwrite = TRUE,
  append = FALSE
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{table}{Character. Working streams table (for BLK scoping).}

\item{to}{Character. Destination table name. Default
\code{"working.features"}.}

\item{points_table}{Character or \code{NULL}. Schema-qualified table with
network-referenced features.}

\item{points}{An \code{sf} object or \code{NULL}. User-provided points to snap
to the network via \code{\link[=frs_point_snap]{frs_point_snap()}}.}

\item{where}{Character or \code{NULL}. SQL predicate to filter
\code{points_table}.}

\item{col_blk}{Character. Column name for stream identifier in
\code{points_table}. Default \code{"blue_line_key"}.}

\item{col_measure}{Character. Column name for route measure in
\code{points_table}. Default \code{"downstream_route_measure"}.}

\item{col_id}{Character or \code{NULL}. Column name for feature ID.
When provided, included in output for joining back to source.}

\item{label}{Character or \code{NULL}. Static label for all features.}

\item{label_col}{Character or \code{NULL}. Column name to read labels from.}

\item{label_map}{Named character vector or \code{NULL}. Maps \code{label_col}
values to output labels.}

\item{overwrite}{Logical. Drop \code{to} before creating. Default \code{TRUE}.}

\item{append}{Logical. INSERT INTO existing \code{to} table. Default
\code{FALSE}.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Locate features from a database table or sf object on the stream
network. Produces a table with \code{blue_line_key}, \code{downstream_route_measure},
\code{label}, and \code{source} columns, suitable for \code{\link[=frs_break_apply]{frs_break_apply()}},
\code{\link[=frs_feature_index]{frs_feature_index()}}, or as a break source in \code{\link[=frs_habitat]{frs_habitat()}}.
}
\details{
Unlike \code{\link[=frs_break_find]{frs_break_find()}} which is specific to gradient threshold
detection, this function handles any point features on the network:
crossings, fish observations, water quality stations, flow gauges,
territory boundaries, etc.
}
\examples{
\dontrun{
conn <- frs_db_conn()

# Crossings with severity labels
frs_feature_find(conn, "working.streams",
  points_table = "working.crossings",
  col_id = "aggregated_crossings_id",
  label_col = "barrier_status",
  label_map = c("BARRIER" = "blocked", "POTENTIAL" = "potential"),
  to = "working.features_crossings")

# Fish observations
frs_feature_find(conn, "working.streams",
  points_table = "bcfishobs.fiss_fish_obsrvtn_events_vw",
  col_id = "fish_observation_point_id",
  label_col = "species_code",
  to = "working.features_fish_obs")

# Use as break source in habitat pipeline
frs_habitat(conn, "BULK", break_sources = list(
  list(table = "working.features_crossings",
       label_col = "label")))

DBI::dbDisconnect(conn)
}
}
\seealso{
Other habitat: 
\code{\link{frs_aggregate}()},
\code{\link{frs_break}()},
\code{\link{frs_break_apply}()},
\code{\link{frs_break_find}()},
\code{\link{frs_break_validate}()},
\code{\link{frs_categorize}()},
\code{\link{frs_classify}()},
\code{\link{frs_cluster}()},
\code{\link{frs_col_generate}()},
\code{\link{frs_col_join}()},
\code{\link{frs_extract}()},
\code{\link{frs_feature_index}()},
\code{\link{frs_habitat}()},
\code{\link{frs_habitat_access}()},
\code{\link{frs_habitat_classify}()},
\code{\link{frs_habitat_partition}()},
\code{\link{frs_habitat_species}()},
\code{\link{frs_network_segment}()}
}
\concept{habitat}
